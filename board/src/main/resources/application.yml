spring:
  datasource: # h2 ?????? ??
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate: # ??? ??? ??? ??, update??
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true    # sql ??? ??? ?? (???? false)
        format_sql: true  # ?? ???? ?? ????? ????? ???
  h2: # H2 ?? ??
    console:
      enabled: true
      path: /h2-console

logging: # ????, sql ?? ? ??? ? ? ?? ???
  level:
    org:
      hibernate:
        SQL: debug

springdoc: # OpenAPI ??(Swagger UI)
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
